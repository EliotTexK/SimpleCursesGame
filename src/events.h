#ifndef EVENTS_H
#define EVENTS_H

// what IDs correspond to what events
enum EventIDs {
    wander,
    moveDirection,
    nothing,
    makeDecision
};

enum InputIDs {
    moveLeft,
    moveRight,
    moveUp,
    moveDown
};

#endif